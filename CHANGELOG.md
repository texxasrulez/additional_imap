# Changelog

All notable changes to `additional_imap` should be documented in this file.

## [Unreleased]

- Ongoing development builds use `additional_imap::PLUGIN_VERSION` with a `+dev` suffix until the next release is cut.

## [1.0.0] - 2026-04-11

- Formalized the plugin's self-metadata through `additional_imap::PLUGIN_VERSION` and `additional_imap::info()`.
- Aligned self-versioning with a cleaner release workflow while keeping existing plugin behavior intact.
